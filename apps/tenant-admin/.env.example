# Tenant Admin — Environment Configuration
#
# VistA connection (optional — if not set, fixture data is used with honest labeling)
# Point to the VistA API (e.g., archive repo API on port 3001)
# VISTA_API_URL=http://127.0.0.1:3001
# VISTA_TIMEOUT_MS=5000

# Server port (default: 4520)
# PORT=4520
