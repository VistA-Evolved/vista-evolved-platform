{
  "_purpose": "DEGRADED-MODE FALLBACK ONLY. These fixture files are NOT the source of truth for VistA-owned data. They exist solely as fallback when the VistA broker is unreachable. See docs/explanation/fixture-inventory-and-truth-ownership-audit.md for the full classification.",
  "_date": "2026-03-21",
  "files": {
    "users.json": {
      "class": "C",
      "classLabel": "degraded-mode test fixture",
      "truthOwner": "VistA File 200 (NEW PERSON)",
      "happyPathRole": "fallback only — VistA-first via ORWU NEWPERS",
      "contents": "6 fabricated user records modeled on File 200 fields"
    },
    "roles.json": {
      "class": "C",
      "classLabel": "degraded-mode test fixture",
      "truthOwner": "VistA File 19.1 (SECURITY KEY)",
      "happyPathRole": "fallback + integration-pending — no bulk key enumeration RPC available",
      "contents": "7 fabricated security key records modeled on File 19.1 fields"
    },
    "facilities.json": {
      "class": "C",
      "classLabel": "degraded-mode test fixture",
      "truthOwner": "VistA Files 4 (INSTITUTION), 40.8 (MEDICAL CENTER DIVISION), 44 (HOSPITAL LOCATION), 42 (WARD LOCATION)",
      "happyPathRole": "fallback only — VistA-first via XUS DIVISION GET + ORWU CLINLOC + ORQPT WARDS",
      "contents": "1 institution hierarchy with divisions, clinics, wards, and beds"
    }
  }
}
