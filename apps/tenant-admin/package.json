{
  "name": "@vista-evolved/tenant-admin",
  "version": "0.0.1",
  "private": true,
  "description": "Tenant Admin workspace — tenant-scoped operational administration shell",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --watch server.mjs",
    "test": "npx playwright test",
    "test:e2e": "npx playwright test --reporter=list"
  },
  "dependencies": {
    "@fastify/static": "^8.1.0",
    "fastify": "^5.3.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
