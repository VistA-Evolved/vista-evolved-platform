{
  "name": "@vista-evolved/terminal-proof",
  "version": "0.0.1",
  "private": true,
  "description": "Minimum browser terminal proof against local-vista distro runtime",
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env src/server.mjs",
    "dev": "node --env-file=.env --watch src/server.mjs"
  },
  "dependencies": {
    "fastify": "^5.3.3",
    "@fastify/websocket": "^11.0.2",
    "@fastify/static": "^8.1.0",
    "ssh2": "^1.16.0"
  }
}
