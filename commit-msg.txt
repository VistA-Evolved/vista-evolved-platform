purge: eliminate all fixture files and fallbacks — VistA-only data policy

Delete all 4 fixture JSON files (users.json, roles.json, facilities.json,
_fixture-manifest.json) and the fixtures/ directory permanently.

Remove fixture fallback logic from all 9 affected routes in server.mjs:
- /users, /users/:userId, /facilities, /facilities/:facilityId
- /roles, /clinics, /wards, /topology, /key-inventory
- /esig-status, /dashboard

Every route now returns VistA data exclusively or an explicit error.
No JSON fallback, no degraded-mode data, no alternate data sources.

Frontend: replace FIXTURE badge with ERROR badge, update CSS and HTML.
Docs: rewrite README, update source-of-truth-index, rewrite fixture-audit doc.
Rules: add .cursor/rules/40-vista-only-data-source.mdc enforcement rule.
AGENTS.md: add non-negotiable rule #8 (VistA-only data for tenant-admin).

Verified: 20/20 routes return source:vista with real VistA data.
Zero fixture references in any API response.
